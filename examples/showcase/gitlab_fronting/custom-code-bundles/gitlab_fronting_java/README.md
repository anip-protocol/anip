# GitLab Fronting Java Bundle

Provider-specific backend adapter for generated Java GitLab fronting services.

The bundle overlays `BackendAdapter.java` and adds a live JUnit smoke that runs
when `GITLAB_TOKEN` and `GITLAB_PROJECT_ID` are configured.
