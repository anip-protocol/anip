# Archived Old Generated GTM Bundles

This folder preserves a copy of the previously generated GTM bundles.

Archived bundles:

- `studio_gtm_pipeline`
- `studio_gtm_enrichment`
- `studio_gtm_prioritization_rest`
- `studio_gtm_outreach_mcp`

The same bundles are intentionally still present under `examples/showcase/gtm/generated` because `docker-compose.yml` and the current showcase runtime build path still depend on those locations.

These are not the manually maintained benchmark sources. The benchmark sources remain under:

- `examples/showcase/gtm/services`
- `examples/showcase/gtm/agents`

The newest Registry/Studio TypeScript generation is:

- `examples/showcase/gtm/generated/studio_gtm_pipeline_typescript_registry`
