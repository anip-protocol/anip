# Jira Fronting C# Custom Bundle

This bundle fills the generated C# backend adapter seam for the Jira fronting showcase.

Runtime credentials:

- `JIRA_BASE_URL`
- `JIRA_EMAIL`
- `JIRA_API_TOKEN`

Live reads call Jira REST. Write-adjacent capabilities return governed previews and never mutate Jira.
