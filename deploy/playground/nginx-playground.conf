# Travel showcase
server {
    listen 80;
    server_name travel.playground.anip.dev;

    location / {
        proxy_pass http://127.0.0.1:9100;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Finance showcase
server {
    listen 80;
    server_name finance.playground.anip.dev;

    location / {
        proxy_pass http://127.0.0.1:9101;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# DevOps showcase
server {
    listen 80;
    server_name devops.playground.anip.dev;

    location / {
        proxy_pass http://127.0.0.1:9102;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Playground landing page
server {
    listen 80;
    server_name playground.anip.dev;

    root /var/www/playground;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
