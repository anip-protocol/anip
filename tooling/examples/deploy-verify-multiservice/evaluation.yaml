evaluation:
  scenario_name: deploy_on_one_service_verify_on_another
  result: PARTIAL
  handled_by_anip:
  - task identity
  - parent invocation lineage
  - durable audit
  - audit queryability
  - cross-service task identity continuity
  - independent but linkable audit records
  - cleaner service handoff
  - verification path guidance (protocol-assisted, advisory, not enforced)
  glue_you_will_still_write:
  - the protocol provides advisory guidance but enforcement still requires wrapper
    logic
  - you will still write cross-service handoff orchestration logic to act on advisory
    hints
  glue_category:
  - orchestration
  why:
  - the design already removes a large amount of cross-service correlation and trace-stitching
    glue
  - "the protocol hints but does not enforce \u2014 advisory surfaces (verify_via\
    \ hint) improve the decision surface but wrapper logic must still act on them"
  what_would_improve:
  - promote advisory orchestration hints to enforceable protocol surfaces
  confidence: high
