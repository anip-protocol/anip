evaluation:
  scenario_name: deploy_service_temporarily_unavailable_wait_path
  result: PARTIAL
  handled_by_anip:
  - task identity
  - parent invocation lineage
  - durable audit
  - audit queryability
  - structured failure
  - recovery class guidance (protocol-assisted, advisory, not enforced)
  glue_you_will_still_write:
  - the protocol provides advisory guidance but enforcement still requires wrapper
    logic
  - "the approach does not declare estimated availability hint \u2014 agents must\
    \ discover these paths through docs or wrapper logic"
  glue_category:
  - orchestration
  why:
  - "the protocol hints but does not enforce \u2014 advisory surfaces (recovery_class)\
    \ improve the decision surface but wrapper logic must still act on them"
  what_would_improve:
  - promote advisory orchestration hints to enforceable protocol surfaces
  confidence: high
