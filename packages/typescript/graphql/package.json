{
  "name": "@anip/graphql",
  "version": "0.8.0",
  "description": "ANIP GraphQL bindings — expose ANIPService capabilities via GraphQL",
  "type": "module",
  "engines": { "node": ">=20" },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@anip/service": "0.8.0",
    "hono": "^4.0.0",
    "graphql": "^16.9.0"
  },
  "devDependencies": {
    "@anip/core": "0.8.0",
    "@anip/hono": "0.8.0",
    "@anip/server": "0.8.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.0"
  }
}
