{
  "name": "@anip/crypto",
  "version": "0.6.0",
  "description": "ANIP cryptographic primitives — key management, JWT, JWS, JWKS",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@anip/core": "0.6.0",
    "jose": "^6.2.1"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^4.1.0"
  }
}
