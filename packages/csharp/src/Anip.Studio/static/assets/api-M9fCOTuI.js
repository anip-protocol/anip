async function e(e){let t=await fetch(`${e}/.well-known/anip`);if(!t.ok)throw Error(`Discovery: ${t.status}`);if(!(t.headers.get(`content-type`)||``).includes(`application/json`))throw Error(`Discovery: non-JSON response. This URL does not look like an ANIP service root.`);return t.json()}async function t(e){let t=await fetch(`${e}/.well-known/jwks.json`);if(!t.ok)throw Error(`JWKS: ${t.status}`);return t.json()}async function n(e,t=20){let n=await fetch(`${e}/anip/checkpoints?limit=${t}`);if(!n.ok)throw Error(`Checkpoints: ${n.status}`);return n.json()}export{e as n,t as r,n as t};